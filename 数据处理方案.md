# 华融协议解析入库 - 数据处理方案

## 一、方案概述

### 1.1 目标

为华融网络协议解析系统设计并实现协议解析入库功能，主要包括：

- **IEC104/IEC101协议解析入库**：四遥数据（遥测、遥信、遥控、遥调）的解析与存储
- **PMU协议解析入库**：相量测量单元（PMU）数据及采样数据的解析与存储
- **TESTER帧入库**：IEC101 IEC104协议测试帧数据入库，用于链路状态监控
- **网监设备告警事件采集**：IEC101 IEC104协议传输事件的采集、解析与存储
- **通道告警事件表**：基于测试帧中断、PMU数据中断的通道告警判定与记录
- **ICMP Ping检测**：设备间周期性ping检测，中断时写入告警事件

### 1.2 功能点清单

| 序号 | 功能点 | 说明 |
|------|--------|------|
| 1 | IEC104协议解析入库 | TCP/IP网络通信协议四遥数据解析存储 |
| 2 | IEC101协议解析入库 | 串口通信协议四遥数据解析存储 |
| 3 | PMU协议解析入库 | 相量测量数据解析存储 |
| 4 | TESTER帧入库 | IEC协议U帧（测试帧）数据入库 |
| 5 | PMU采样数据入库 | PMU原始采样波形数据入库 |
| 6 | 网监设备告警事件采集 | IEC协议传输异常事件采集 |
| 7 | 通道告警事件表 | 基于测试帧中断/PMU数据中断的告警判定 |
| 8 | ICMP Ping检测 | 设备间周期性ping检测，中断写入告警 |

### 1.3 应用场景

- **IEC设备连接状态监控**：通过测试帧表判断设备是否在线、连接是否正常
- **四遥数据辅助分析**：提供遥测、遥信、遥控、遥调的详细数据用于业务分析
- **PMU数据辅助分析**：提供相量测量数据用于频率监测、相角分析
- **PMU采样分析**：提供原始采样波形数据用于电能质量分析和故障录波
- **通道状态监控**：通过告警事件表实时监控通信通道健康状态
- **网络连通性监控**：通过ICMP Ping检测设备间网络连通性

---

## 二、数据表设计方案

### 2.1 IEC协议测试帧表（核心表）

详见拆分文档：[数据处理方案/taos表/iec_test_frame.md](数据处理方案/taos表/iec_test_frame.md)

---

### 2.2 四遥数据表

详见拆分文档：[数据处理方案/taos表/four_remote_data.md](数据处理方案/taos表/four_remote_data.md)

---

### 2.3 PMU数据表

详见拆分文档：[数据处理方案/taos表/pmu_measurement_detail.md](数据处理方案/taos表/pmu_measurement_detail.md)

---

### 2.5 网监设备安全事件表

详见拆分文档：[数据处理方案/taos表/security_events.md](数据处理方案/taos表/security_events.md)

---

### 2.6 通道告警事件表

详见拆分文档：[数据处理方案/taos表/channel_alarm_event.md](数据处理方案/taos表/channel_alarm_event.md)

---

### 2.7 ICMP Ping检测

#### 2.7.1 功能说明

系统支持对配置的设备进行周期性ICMP Ping检测，用于监控设备间网络连通性。当Ping检测中断时，写入通道告警事件表。

#### 2.7.2 Ping检测配置表（MySQL）

详见拆分文档：[数据处理方案/mysql表/ping_monitor_config.md](数据处理方案/mysql表/ping_monitor_config.md)

#### 2.7.3 Ping检测结果表（TDengine）

详见拆分文档：[数据处理方案/taos表/ping_detect_result.md](数据处理方案/taos表/ping_detect_result.md)

#### 2.7.4 Ping检测与告警联动流程

```text
Ping检测流程：

1. 启动时从ping_monitor_config加载enabled=true的配置
2. 为每个目标IP创建定时任务，按ping_interval周期执行
3. 每次检测：
   a. 执行ICMP Ping（优先InetAddress.isReachable()，备选系统ping命令）
   b. 记录结果写入ping_detect_result表
   c. 更新连续失败/成功计数器（内存维护）
4. 告警判定：
   - 连续失败次数 >= fail_threshold -> 产生告警
     写入channel_alarm_event（alarm_type=PING_INTERRUPT, judge_type=PING_BASED）
   - 连续成功次数 >= recover_threshold -> 恢复告警
     更新channel_alarm_event（is_recovered=true, recover_time=当前时间）
5. 配置热更新：支持通过REST API动态增删改Ping目标，无需重启
```

---

### 2.8 IEC101协议支持

#### 2.8.1 IEC101与IEC104的区别

| 特性 | IEC101 | IEC104 |
| ---- | ------ | ------ |
| 传输介质 | 串口（RS232/RS485） | TCP/IP网络 |
| 帧结构 | FT1.2帧格式 | APCI+ASDU |
| 启动字符 | 0x10（固定帧）、0x68（可变帧） | 0x68 |
| 链路层 | 有链路层（平衡/非平衡模式） | 无链路层 |
| 测试帧 | 链路测试（功能码9） | U帧测试（TESTFR） |
| 地址长度 | 链路地址（1-2字节） | 无链路地址 |
| 应用层 | ASDU结构相同 | ASDU结构相同 |

#### 2.8.2 IEC101帧格式

**固定帧格式**：

```text
+------+------+------+------+------+
| 0x10 |  C   |  A   | CS   | 0x16 |
+------+------+------+------+------+
  启动  控制域 链路地址 校验和  结束
```

**可变帧格式**：

```text
+------+------+------+------+------+------+------+-----+------+------+
| 0x68 |  L   |  L   | 0x68 |  C   |  A   | ASDU | ... | CS   | 0x16 |
+------+------+------+------+------+------+------+-----+------+------+
  启动  长度   长度   启动   控制域 链路地址 应用数据      校验和  结束
```

#### 2.8.3 IEC101链路测试帧表

详见拆分文档：[数据处理方案/taos表/iec101_link_test.md](数据处理方案/taos表/iec101_link_test.md)

---

